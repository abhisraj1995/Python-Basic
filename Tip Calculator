# Tip Calculator

print('welcome to the tip calculator.')

total_bill=float(input('Wat was the total bill '))

tip=int(input('what percentage tip you like to give? 10,12 or 15 ? '))

total_bill_per=total_bill+(total_bill*tip/100)

people=int(input('how many to split the bill ? '))

pay=total_bill_per/people

print('Each person should pay : ' , round(pay,2))
